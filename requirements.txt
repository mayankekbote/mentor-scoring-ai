# MentorAI — Full Stack Dependencies
# Python 3.10+

# ── Environment Variables ──────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── AI Engine (existing pipeline) ─────────────────────────────────────────
openai>=1.0.0
groq>=0.4.0
mediapipe==0.10.9
opencv-python>=4.8.0
librosa>=0.10.0
soundfile>=0.12.0
ffmpeg-python>=0.2.0
numpy>=1.24.0,<2.0.0
protobuf>=3.11,<4

# ── Backend — FastAPI ──────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9       # file uploads
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── Database — PostgreSQL + SQLAlchemy ────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# ── Auth ───────────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
